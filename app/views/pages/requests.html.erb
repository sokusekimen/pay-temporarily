<% @page_title = 'リクエスト一覧' %>
<h1>申請されているリクエスト一覧</h1>

<% @users.each do |user| %>
  <hr>
  <p><span>name: </span><%= link_to user.username, page_path(user.id) %></p>
  <p><span>email: </span><%= link_to user.email, page_path(user.id) %></p>
  <p><%=link_to "フレンドになる", page_friendships_path(user.id), method: :post %></p>
<% end %>